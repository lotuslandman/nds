
  
<h1>Plotting:
<%= "Response Time" if session[:y_axis] == "response_time" %>
<%= "Number of NOTAMs" if session[:y_axis] == "number_of_notams" %>
 in 
<%= "FNTB (.75)" if session[:env] == "fntb" %>
<%= "Production (OEX)" if session[:env] == "prod" %>
<%= "FNTB (testset)" if session[:env] == "fntb_test" %>
</h1>
Change (<a href="fntb">FNTB</a> <a href="prod">Production</a> <a href="fntb_test">FNTB testset</a>)<br>
Change (<a href="response_time">response time</a> <a href="number_of_notams"># of NOTAMs</a>)

<%= javascript_include_tag "https://www.gstatic.com/charts/loader.js" %>

<%= column_chart @get_column_chart_data %>

<%= form_tag(controller: "graph", action: "graph", method: "get") do %>
  <%= label_tag(:q, "Start:") %>
  <%= text_field_tag(:start_graph) %>
  <%= label_tag(:q, "End:") %>
  <%= text_field_tag(:end_graph) %>
  <%= label_tag(:q, "Scenario:") %>
  <%= text_field_tag(:scenario) %>
  <%= submit_tag("Submit") %>
<% end %>
Half Range: <a href="left_half"><</a> <a href="right_half">></a><br>
Expand Range: <a href="expand_left"><<</a> <a href="expand_right">>></a><br>

start: <%= (Time.now - (60*10*1)).to_s  %> ---- end: <%= Time.now.to_s %> Example: 10 minutes back<br>
start: <%= (Time.now - (60*60*1)).to_s  %> ---- end: <%= Time.now.to_s %> Example: 1 Hour back<br>
start: <%= (Time.now - (60*60*6)).to_s  %> ---- end: <%= Time.now.to_s %> Example: 6 Hours back<br>
start: <%= (Time.now - (60*60*24)).to_s  %> ---- end: <%= Time.now.to_s %> Example: 24 Hours back<br>

<h4 style="color:silver">Date Range:</h4>
  <table style="width:100">
    <colgroup>
      <col style="background-color: silver">
      <col style="background-color: silver">
    </colgroup>
  <tr>
    <th>Start Date:</th>
    <th>End Date:</th> 
  </tr>
  <tr>
    <td><i><%= session[:start_date].to_s %></i> </td> 
    <td><i><%= session[:end_date].to_s %></i> </td> 
  </tr>
</table>
<h4 style="color:blue">Blue FILTER BY:</h4>
  <table style="width:100">
    <colgroup>
      <col style="background-color: silver">
      <col style="background-color: silver">
    </colgroup>
  <tr>
    <th>Filter</th>
    <th>Filter Status</th> 
  </tr>
  <tr>
    <td>Scenario</td>
    <td><i>None</i> </td> 
  </tr>
  <tr>
    <td>xsi:nil="true" present</td>
    <td><i>No</i> </td> 
  </tr>
  <tr>
    <td>bad href with #</td>
    <td><i>No</i> </td> 
  </tr>
  <tr>
    <td>href missing pointer</td>
    <td><i>No</i> </td> 
  </tr>
  <tr>
    <td>FICON</td>
    <td><i>No</i> </td> 
  </tr>
</table>

<h4 style="color:red">Red FILTER BY:</h4>
  <table style="width:100">
    <colgroup>
      <col style="background-color: silver">
      <col style="background-color: silver">
    </colgroup>
  <tr>
    <th>Filter</th>
    <th>Filter Status</th> 
  </tr>
  <tr>
    <td>Scenario</td>
    <td><i><%= @scenario %> </i> </td> 
  </tr>
  <tr>
    <td>xsi:nil="true" present</td>
    <td><i>No</i> </td> 
  </tr>
  <tr>
    <td>bad href with #</td>
    <td><i>No</i> </td> 
  </tr>
  <tr>
    <td>href missing pointer</td>
    <td><i>No</i> </td> 
  </tr>
  <tr>
    <td>FICON</td>
    <td><i>No</i> </td> 
  </tr>
</table>
